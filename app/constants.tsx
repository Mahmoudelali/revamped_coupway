export enum BottomTabScreens {
	HOME = 'home',
	SEARCH = 'search',
	NOTIFICATIONS = 'notifications',
	BASKET = 'basket',
	PROFILE = 'profile',
}

export interface responseModel {
	prodName: string;
}
